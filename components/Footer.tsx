export function Footer() {
  return (
    <footer className="footer container">
      <small>
        © {new Date().getFullYear()} Football Hub — Community football news and fixtures.
      </small>
    </footer>
  );
}
